# JPG Optimizer Pro 🖼

Професійний інструмент для оптимізації JPG зображень на macOS.

## ✨ Можливості

- **Три режими оптимізації:**
  - 🔒 **Lossless** — без втрат якості (оптимізація Huffman таблиць)
  - ⚖️ **Balanced** — баланс якості та розміру (налаштовуваний 60-100%)
  - 📦 **Maximum** — максимальне стиснення для веб

- **MozJPEG підтримка** — на 10-15% краще стиснення при тій же якості
- **Перемикач MozJPEG** — можна вимкнути для швидкого тесту
- **Навігатор порівняння** — перегляд До/Після як у Photoshop
- **Вибір папки збереження** — зберігати в окрему папку
- **Шаблони імен файлів** — `{name}`, `{date}`, `{counter}`
- **Збереження EXIF** — метадані зберігаються або видаляються
- **Паралельна обробка** — використовує всі ядра CPU

## 📋 Вимоги

- macOS 10.15+ (Catalina або новіше)
- Python 3.8+

## 🚀 Встановлення

### Швидке встановлення

```bash
cd JPGOptimizerPro
./install.sh
```

Скрипт автоматично:
1. Перевірить Python 3
2. Встановить бібліотеки (Pillow, piexif)
3. Запропонує встановити MozJPEG
4. Скопіює додаток в /Applications

### Ручне встановлення

1. Встановіть залежності:
```bash
pip3 install Pillow piexif
```

2. (Опціонально) Встановіть MozJPEG для кращого стиснення:
```bash
brew install mozjpeg
```

3. Запустіть:
```bash
python3 jpg_optimizer_pro.py
```

Або двічі клікніть на `JPG Optimizer Pro.app`

## 📖 Використання

1. **Додайте файли** — перетягніть або використайте кнопки
2. **Виберіть режим** — Lossless, Balanced або Maximum
3. **Налаштуйте якість** (для Balanced) — 85% оптимально
4. **Виберіть папку** збереження (опціонально)
5. **Натисніть "Оптимізувати"**
6. **Порівняйте результат** — кнопка "Порівняти до/після"

## 🔧 Налаштування

### Шаблони імен файлів

| Шаблон | Результат |
|--------|-----------|
| `{name}` | Оригінальне ім'я (перезапис) |
| `{name}_optimized` | photo_optimized.jpg |
| `{name}_opt_{date}` | photo_opt_20240210.jpg |
| `{date}_{name}` | 20240210_photo.jpg |
| `optimized_{counter}_{name}` | optimized_0001_photo.jpg |

### Вкладка "Налаштування"

- **Видалити EXIF** — зменшує розмір, але втрачає дату/GPS
- **Зберігати структуру папок** — при виборі папки
- **Перезаписувати оригінали** — замість створення нових

## 📊 Рекомендації щодо якості

| Мета | Режим | Якість | Очікувана економія |
|------|-------|--------|-------------------|
| Архів фото | Lossless | — | 5-15% |
| Архів (компактний) | Balanced | 90-95% | 20-40% |
| Загальне використання | Balanced | 80-85% | 40-60% |
| Веб / Email | Maximum | 70% | 60-80% |

## 🐛 Вирішення проблем

### Додаток не запускається
```bash
# Зняти карантин macOS
xattr -rd com.apple.quarantine "JPG Optimizer Pro.app"
```

### MozJPEG не працює
```bash
# Перевірити встановлення
ls /opt/homebrew/opt/mozjpeg/bin/
# Повинні бути: cjpeg, djpeg, jpegtran
```

### Помилка "Python not found"
```bash
# Встановити Python
brew install python3
# або завантажити з python.org
```

## 📁 Структура проекту

```
JPGOptimizerPro/
├── JPG Optimizer Pro.app/  # macOS додаток
├── jpg_optimizer_pro.py    # Основний код
├── install.sh              # Скрипт встановлення
├── run.command             # Швидкий запуск
└── README.md               # Документація
```

## 📄 Ліцензія

Вільне використання для особистих та комерційних цілей.

---
Створено за допомогою Claude 🤖
